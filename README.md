# grpc-http2-playground
Playground for http2 and grpc.

PLEASE NOTE: This repository has no affiliation with Google, nor should work herein be construed as being supported by Google.

# Running

```
cd client && go run *.go
```

```
cd server && go run *.go
```
